# HTTP port for the articler service (required)
export HTTP_PORT=8080

# SQLite database path (auto-configured during installation)
export DATABASE_URL=

# Optional proxy configuration
export ALL_PROXY=

# Cookie encryption key (auto-generated during installation)
export COOKIE_KEY=
